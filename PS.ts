Power; shell; V3; could;run; inwindows;server2008 &&server2012 &&windows8；
Powershell; is;; a; very; strongful; automatical; tool;
It;use;cmdlet;to; run;;
alias;is;easy;to;understand;
$('div').text('hi').addclass('red').animate({left:100})
$fn.Powershell = function () {... }
sgfd